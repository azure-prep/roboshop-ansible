- name: Set up java
  ansible.builtin.include_role:
    name: common
    tasks_from: java

- name: Install mysql
  ansible.builtin.dnf:
    name: mysql

#- name: Load Schema, Schema in database is the structure to it like what tables to be created and their necessary application layouts
#  ansible.builtin.shell:
#    - mysql -h mysql-dev.azdevopsb1.online -uroot -pRoboShop@1 < /app/db/schema.sql
#    - mysql -h mysql-dev.azdevopsb1.online -uroot -pRoboShop@1 < /app/db/app-user.sql
#    - mysql -h mysql-dev.azdevopsb1.online -uroot -pRoboShop@1 < /app/db/master-data.sql

- name: Start systemd service
  ansible.builtin.include_role:
    name: common
    tasks_from: systemd_setup.yaml